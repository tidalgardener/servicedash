# Rules

## Non-negotiables (security, secrets, style, constraints)
- Do not commit secrets, tokens, or credentials.
- Keep local data in-repo (ignored by git) unless explicitly requested otherwise.

## “Do not do” list (if any)
- Do not call AWS APIs that require credentials.

