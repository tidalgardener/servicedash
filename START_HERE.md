# Start Here

## Read order
1) `START_HERE.md`
2) `CONTINUITY.md` (canonical session ledger)
3) `README.md`

## Local setup (TBD)
- `python3 -m venv .venv`
- `. .venv/bin/activate`
- `python -m pip install -r requirements.txt`

## How to run (TBD)
- `python -m servicedash run`
- Optional (headless poller): `python -m servicedash poll --log`
- Config: `servicedash.json` (poll interval, services, DB path)
- Terminal: needs at least `80x25` (recommended ~`80x80`)

## Where to start / current focus
- The current goal, constraints, and next actions live in `CONTINUITY.md`.
