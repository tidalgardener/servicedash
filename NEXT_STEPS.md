# Next Steps
1) Run the headless poller for a while: `python -m servicedash poll --log`
2) Open the UI to see a filled 24h trend: `python -m servicedash run`
3) Customize `servicedash.json` (poll interval, add/remove services, optional `"group"` overrides).
