# TODO

## P0 (must do next)
- Optional: export last 24h to JSON/CSV.
- Optional: add “Claude Console” row (`platform.claude.com`) if you care about the web console separately.

## P1
- Add launchd helper (optional) to run `servicedash poll` on login.
- Improve “AWS” signal (region-specific filters; UNCONFIRMED).

## P2
- Optional notifications (desktop or terminal bell) on non-OK status (UNCONFIRMED).
